import React, { Component, Fragment } from 'react';
import { FlexButton } from '../src';

const DisplayButtons = (props) =>(<Fragment>
     <p className="App-intro">
          All Buttons
    </p>
    <FlexButton  />
</Fragment>);

export default DisplayButtons;
